import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.registrodeobras.app",
  appName: "Registro de Obras y Certificación",
  webDir: "../../packages/app/dist",
};

export default config;
